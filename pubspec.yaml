name: inner_breeze
description: Intuitive breathing trackr

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  shared_preferences: ^2.1.2
  table_calendar: ^3.0.9
  provider: ^6.0.0
  go_router: ^10.0.0
  syncfusion_flutter_sliders: ^22.2.7 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/